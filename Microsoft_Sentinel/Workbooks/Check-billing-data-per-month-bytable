search *
| where TimeGenerated >= ago(30d)
| where _IsBillable == "True"
| summarize BillableGB = round(sum(_BilledSize) / 1024.0 / 1024.0 / 1024.0, 2) by $table
| order by BillableGB desc
| render areachart  